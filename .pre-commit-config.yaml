# S0.1.2 — the same three gates CI runs, on the way into a commit.
# `pre-commit run --all-files` must exit 0.
default_language_version:
  python: python3.12

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-added-large-files
        args: ["--maxkb=512"]
      - id: check-merge-conflict
      - id: check-toml
      - id: check-yaml
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: mixed-line-ending
        args: ["--fix=lf"]
      - id: trailing-whitespace

  # Pinned to the same ruff the venv and CI run (`uv run ruff --version`). Two different
  # ruff versions gating one tree means the hook and `make lint` can disagree, which is how
  # a lint failure reaches CI green. `tests/unit/test_toolchain_config.py` asserts they match.
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.2
    hooks:
      - id: ruff-check
      - id: ruff-format

  - repo: local
    hooks:
      - id: mypy
        name: mypy (strict)
        entry: uv run mypy
        language: system
        pass_filenames: false
        types: [python]
